<h1>Message#index</h1>

<turbo-frame id="new_message">
<%= form_for(@message) do |f| %>
<%= f.text_area :message %>
<%= f.submit %>
<% end %>
</turbo-frame>

<turbo-frame id="count" src="/messages/count">
  fuga
</turbo-frame>

<% @messages.each do |m| %>
  <%= render partial:'messages/message', locals:{message:m} %>
<% end %>

<turbo-frame id="messages" src="/messages">
</turbo-frame>
<br />
<%= DateTime.now %>